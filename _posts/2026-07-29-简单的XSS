---
layout: default
title: **简单的 XSS.md”看前端逻辑漏洞与客户端信任危机**
date: 2026-07-29 09:00:00 +0800
---

# 我的第二篇文章

看到要打满1000个怪物真的要打吗？NO NO NO,我们又不是donk。。。

题目告诉我们这是一个简单的XSS题目，可以知道，这个题目有关web前端安全，我们可以从这一步来入手。

查看源代码，看到外部JS文件2. `challenge.js`，挑战逻辑可能就在其中。
我们打开这个文件审计代码，发现：
函数 `getFlag` 的权限验证仅依赖传入的 `score` 参数，且 **完全由客户端控制**。
函数被直接暴露到全局作用域 `window` 上，这意味着 **任何拥有控制台权限的人都可以直接调用它**
1. **逻辑漏洞本质**
游戏要求玩家通过操作（如射击僵尸）积累分数，但**服务端并未对分数来源进行二次验证**。攻击者可以绕过游戏逻辑，直接操纵分数参数。
2. **作用域泄露与XSS关联**
`window.getFlag = getFlag` 这行代码将敏感函数暴露为全局变量。虽然题目名为“简单的XSS”，但本题并未直接涉及跨站脚本攻击。然而，如果存在DOM型XSS漏洞，攻击者可以注入恶意脚本调用 `getFlag(1000)`，从而窃取Flag。这里的作用域泄露为潜在攻击提供了入口点。
**解题步骤：3秒获取Flag**

1. 打开游戏页面，按 `F12` 打开开发者工具，切换到 **Console** 面板。
2. 在控制台输入以下命令并回车：
3. 瞬间弹出一个Alert窗口，显示Flag值，复制并提交即可。

 **核心收获**
    - **CTF思维**：面对看似不可能的任务，优先考虑审计代码而非盲目执行。
    - **安全开发**：前端逻辑仅作为用户体验优化，所有关键验证必须服务端兜底。
    - **代码安全**：避免将敏感操作暴露到全局作用域（如 `window`），防止逻辑绕过。
 **漏洞修复建议**
若为开发者，应：
    - 在服务端验证用户分数是否通过合法游戏流程获得（如存储在Session中）。
    - 移除 `window.getFlag = getFlag`，限制函数作用域。
